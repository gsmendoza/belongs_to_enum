sqlite:
  :adapter: sqlite
  :dbfile: vendor/plugins/belongs_to_enum/test/belongs_to_enum.sqlite.db

sqlite3:
  :adapter: sqlite3
  :dbfile: vendor/plugins/belongs_to_enum/test/belongs_to_enum.sqlite3.db

postgresql:
  :adapter: postgresql
  :username: george
  :password:
  :database: belongs_to_enum_test
  :min_messages: ERROR

mysql:
  :adapter: mysql
  :host: localhost
  :username: root
  :password:
  :database: belongs_to_enum_test
